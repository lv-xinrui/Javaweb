<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.javaweb01.mapper.StudentMapper">

    <!-- 学生结果映射 -->
    <resultMap id="StudentResultMap" type="com.example.javaweb01.entity.Student">
        <id column="id" property="id"/>
        <result column="student_id" property="studentId"/>
        <result column="name" property="name"/>
        <result column="password" property="password"/>
        <result column="gender" property="gender"/>
        <result column="major" property="major"/>
        <result column="class_name" property="className"/>
        <result column="grade" property="grade"/>
        <result column="phone" property="phone"/>
        <result column="email" property="email"/>
        <result column="status" property="status"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <!-- 根据学号查询学生信息 -->
    <select id="findByStudentId" resultMap="StudentResultMap">
        SELECT * FROM students WHERE student_id = #{studentId} AND status = 1
    </select>

    <!-- 根据学号和密码验证登录 -->
    <select id="findByStudentIdAndPassword" resultMap="StudentResultMap">
        SELECT * FROM students 
        WHERE student_id = #{studentId} AND password = #{password} AND status = 1
    </select>

    <!-- 插入学生信息 -->
    <insert id="insert" parameterType="com.example.javaweb01.entity.Student" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO students (student_id, name, password, gender, major, class_name, grade, phone, email, status, create_time)
        VALUES (#{studentId}, #{name}, #{password}, #{gender}, #{major}, #{className}, #{grade}, #{phone}, #{email}, #{status}, #{createTime})
    </insert>

    <!-- 更新学生信息 -->
    <update id="update" parameterType="com.example.javaweb01.entity.Student">
        UPDATE students SET
            name = #{name},
            gender = #{gender},
            major = #{major},
            class_name = #{className},
            grade = #{grade},
            phone = #{phone},
            email = #{email},
            update_time = #{updateTime}
        WHERE student_id = #{studentId}
    </update>

</mapper>
